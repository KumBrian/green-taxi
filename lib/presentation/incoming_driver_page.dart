import 'package:flutter/material.dart';

class IncomingDriverPage extends StatelessWidget {
  const IncomingDriverPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Incoming Driver Page')));
  }
}
